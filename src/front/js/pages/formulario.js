import React, { useContext } from "react";
import { Context } from "../store/appContext";
import "../../styles/home.css";

	export const Formulario = () => {
		const { store, actions } = useContext(Context);
		return (
			<div><h1>QUE PASA</h1></div>

    );
};